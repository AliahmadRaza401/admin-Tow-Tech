import 'package:flutter/material.dart';

class TechnicianMobile extends StatelessWidget {
  // const TechnicianMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Driver Mobile"),
    );
  }
}

